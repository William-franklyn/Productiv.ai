import { ComingSoon } from "@/components/shell/ComingSoon";

export const metadata = { title: "Assistant" };

export default function AssistantPage() {
  return (
    <ComingSoon
      title="Assistant"
      body="The chat assistant is being wired up next — it will answer from your knowledge base, create tasks, and chart data on request."
    />
  );
}
