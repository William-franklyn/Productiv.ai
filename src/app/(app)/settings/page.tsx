import { ComingSoon } from "@/components/shell/ComingSoon";

export const metadata = { title: "Settings" };

export default function SettingsPage() {
  return (
    <ComingSoon
      title="Settings"
      body="Workspace and account settings live here."
    />
  );
}
